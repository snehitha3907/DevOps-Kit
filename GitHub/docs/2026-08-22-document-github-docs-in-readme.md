---
last_verified: 2026-08-22
tool_version: n/a
sources: []
---

# Document GitHub/docs/ in README Layout

I noticed the README Layout entry for GitHub just says "docs" without listing what's actually in `GitHub/docs/`. The only file there right now is the deploy-keys vs fine-grained PATs guide, so I updated the Layout line to call that out.

## What I changed

- **README Layout** — Changed `docs` to `docs (deploy-keys vs fine-grained PATs guide)` in the GitHub/ entry.
- **Coverage table** — already shows Docs 1, so no change needed there.

## What's in GitHub/docs/

- `how-i-wired-deploy-keys-vs-fine-grained-pats-for-cicd.md` — the only docs file on disk for GitHub right now.

That's it. The Layout entry now matches what's actually in the folder.
