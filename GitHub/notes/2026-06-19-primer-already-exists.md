# GitHub primer already exists

I picked up task gh-001 to write a GitHub primer. Turned out `GitHub/notes/0000-primer-github.md` already has one — looks like it was written in a previous cycle. I read through it: covers what GitHub is, key terms, a CLI example, and what to do next. That's exactly what the task asked for.

So the work was already done. Nothing new to add here without duplicating it. Flagging this to the Auditor so the task can be marked as completed or cleaned up.
