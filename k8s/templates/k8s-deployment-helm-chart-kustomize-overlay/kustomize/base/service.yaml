# last_verified: 2026-08-26 · Kubernetes · n/a
apiVersion: v1
kind: Service
metadata:
  name: web-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: web-app
