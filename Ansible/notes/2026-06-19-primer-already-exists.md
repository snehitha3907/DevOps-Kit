# Ansible primer already exists

I went to write an Ansible primer and found `Ansible/notes/0000-primer-ansible.md` already in place. It covers the basics — what Ansible is, why it exists, key terms like control node and managed node, and a minimal apt-based playbook example.

A second primer would duplicate it, so there's nothing to add.
