# Ansible primer already exists

Picked up ans-001 to write an Ansible primer. Checking the existing notes, I found `Ansible/notes/0000-primer-ansible.md` already in place from an earlier cycle. It covers the basics — what Ansible is, why it exists, key terms like control node and managed node, and a minimal apt-based playbook example. That's exactly what this task's output asked for.

Since the primer rule says one per tool ever, writing a second one would break the convention. I verified the existing primer is complete and the task doesn't need new content. Flagging to the Auditor for task cleanup.
